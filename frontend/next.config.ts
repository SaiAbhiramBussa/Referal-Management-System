import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // Disable telemetry for production
  // Add any additional production optimizations here
};

export default nextConfig;
